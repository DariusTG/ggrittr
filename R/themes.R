theme_bw_p = t_bw = function(p, ...) {return(p + theme_bw(...))}
theme_classic_p = t_classic = function(p, ...) {return(p + theme_classic(...))}
theme_dark_p = t_dark = function(p, ...) {return(p + theme_dark(...))}
theme_get_p = t_get = function(p, ...) {return(p + theme_get(...))}
theme_gray_p = t_gray = function(p, ...) {return(p + theme_gray(...))}
theme_grey_p = t_grey = function(p, ...) {return(p + theme_grey(...))}
theme_light_p = t_light = function(p, ...) {return(p + theme_light(...))}
theme_linedraw_p = t_linedraw = function(p, ...) {return(p + theme_linedraw(...))}
theme_minimal_p = t_minimal = function(p, ...) {return(p + theme_minimal(...))}
theme_replace_p = t_replace = function(p, ...) {return(p + theme_replace(...))}
theme_set_p = t_set = function(p, ...) {return(p + theme_set(...))}
theme_update_p = t_update = function(p, ...) {return(p + theme_update(...))}
theme_void_p = t_void = function(p, ...) {return(p + theme_void(...))}
theme_p = function(p, ...) {return(p + geom_blank(...))}
