guides_p = function(p, ...) {return(p + guides(...))}
labs_p = function(p, ...) {return(p + labs(...))}
ggtitle_p = function(p, ...) {return(p + ggtitle(...))}
xlab_p = function(p, ...) {return(p + xlab(...))}
ylab_p = function(p, ...) {return(p + ylab(...))}
annotate_p = function(p, ...) {return(p + annotate(...))}
